package com.edward.game;

// To render objects in camera view: g.fillRect(x + offsetX, y + offsetY, w, h);
public class Camera {
	public double x = 0;
	public double y = 10;
	
	public void tick() {
		
	}
}
